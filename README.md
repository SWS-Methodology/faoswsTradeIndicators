Files starting with 'ti_' are the plugins' scripts.

plugin_helper_functions.R contains some custom-made functions used within the plugins' scripts.

renv.lock is the renv lock file used within the plugins.
